# Upgrade to v1.9.0

This release adds PDF page/range selection and selected-page-aware PDF export.

For an existing Pinokio installation, stop the app and overwrite the files from the v1.9.0 patch. Do not delete `env/`, `models/`, or `offload/`. No model redownload is required.
